#include <iostream>
#include <unordered_map>

using namespace std;

int main() {
    int n;
    cin >> n;

    unordered_map<int, int> counts;

    for (int i = 0; i < n; ++i) {
        int num;
        cin >> num;
        counts[num]++;
    }

   
    for (int i = 0; i < n + 1; ++i) {
        int num;
        cin >> num;
        counts[num]--;
    }

    for (auto it : counts) {
        if (it.second < 0) {
            cout << it.first << endl;
            break;
        }
    }

    return 0;
}
